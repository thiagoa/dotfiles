#!/bin/zsh

if [[ "$@" == '' ]]; then
  if [ -f ~/.reuse ]; then
    echo "Running: $(cat ~/.reuse)"
    echo "-----------"

    zsh ~/.reuse
  fi
else
  PROCESS=$(ps ax | grep $$ | grep -v "grep $$")
  COMMAND=${PROCESS:59}

  echo $COMMAND > ~/.reuse
fi
