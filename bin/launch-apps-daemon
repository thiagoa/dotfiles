#!/usr/bin/env bash

OPEN_APPS_CACHE="$HOME/.open_apps_enabled"

while true; do
  sleep 5

  if [[ -f $OPEN_APPS_CACHE ]]; then
    for app in $(cat $OPEN_APPS_CACHE); do
      gtk-launch $app
    done

    rm -f $OPEN_APPS_CACHE
  fi
done
