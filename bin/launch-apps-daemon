#!/usr/bin/env bash

OPEN_APPS_CACHE="/home/thiago/.chrome_apps_enabled"

while true; do
  sleep 5

  if [[ -f $OPEN_APPS_CACHE ]]; then
    for app in $(cat $OPEN_APPS_CACHE); do
      DISPLAY=:1 gtk-launch $app
    done

    rm -f $OPEN_APPS_CACHE
  fi
done
